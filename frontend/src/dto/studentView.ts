export type StudentView = {
  id: string;
  firstName: string;
  lastName: string;
  section: string;
  studentIdNo: string;
  seatRow: 0;
  seatCol: 0;
};

export type StudentListView = StudentView[];
